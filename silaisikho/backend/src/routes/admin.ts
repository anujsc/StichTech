import { Router } from 'express';

const router = Router();

// Admin routes will be added here as needed
// Admins are created directly in the database, not via API
// This prevents unauthorized admin creation

export default router;
