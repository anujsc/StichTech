// TODO: generateAccessToken(payload) — signs JWT with JWT_ACCESS_SECRET
// TODO: generateRefreshToken(payload) — signs JWT with JWT_REFRESH_SECRET
// TODO: verifyAccessToken(token) — verifies and decodes access token
// TODO: verifyRefreshToken(token) — verifies and decodes refresh token
